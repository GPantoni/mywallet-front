import styled from "styled-components";

const Span = styled.span`
    font-style: normal;
    font-weight: bold;
    font-size: 15px;
    line-height: 18px;

    color: #FFFFFF;
`

export default Span;